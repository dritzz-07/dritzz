{
  "entities": {
    "User": {
      "title": "User",
      "description": "User profile information",
      "type": "object",
      "properties": {
        "uid": { "type": "string", "description": "Unique identifier for the user" },
        "email": { "type": "string", "description": "User email address" },
        "displayName": { "type": "string", "description": "User display name" },
        "photoURL": { "type": "string", "description": "User profile photo URL" },
        "phoneNumber": { "type": "string", "description": "User contact number" },
        "createdAt": { "type": "string", "format": "date-time", "description": "Timestamp of account creation" },
        "lastUpdated": { "type": "string", "format": "date-time", "description": "Timestamp of last profile update" }
      },
      "required": ["uid", "email", "createdAt"]
    },
    "Booking": {
      "title": "Booking",
      "description": "Car wash booking details",
      "type": "object",
      "properties": {
        "userId": { "type": "string", "description": "Reference to the User who made the booking" },
        "name": { "type": "string" },
        "phone": { "type": "string" },
        "vehicleType": { "type": "string", "enum": ["hatchback", "sedan", "suv"] },
        "packageId": { "type": "string" },
        "address": { "type": "string" },
        "date": { "type": "string" },
        "time": { "type": "string" },
        "status": { "type": "string", "enum": ["pending", "confirmed", "completed", "cancelled"] },
        "amount": { "type": "number" },
        "refId": { "type": "string" },
        "createdAt": { "type": "string", "format": "date-time" },
        "updatedAt": { "type": "string", "format": "date-time" }
      },
      "required": ["userId", "name", "phone", "vehicleType", "packageId", "address", "date", "time", "status", "amount", "refId", "createdAt"]
    }
  },
  "firestore": {
    "/users/{userId}": {
      "schema": "User",
      "description": "Stores user profile data"
    },
    "/bookings/{bookingId}": {
      "schema": "Booking",
      "description": "Stores car wash bookings"
    }
  }
}
